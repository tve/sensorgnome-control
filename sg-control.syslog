#! /bin/bash

egrep -q sg-control /etc/rsyslog.conf && exit 0

sed -i -e '/log\/syslog/s/authpriv.none/authpriv.none,local6.none/' /etc/rsyslog.conf
tee -a /etc/rsyslog.conf <<EOF

# Sensorgnome sg-control
local6.* -/var/log/sg-control.log
EOF
echo "rsyslog config patched"
